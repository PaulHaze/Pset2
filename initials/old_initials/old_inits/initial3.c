#include <cs50.h>
#include <stdio.h>
#include <string.h>

int c = 1;
//char initial = 0;

int main(void)
{
    string name = get_string();
    
        for (int i = 0, n = strlen(name); i < n; i++)
            {
              char init[] = name[i];
            
       
            }
        }    
    //printf("first inital: %c\n", firstInit);
    printf("\n");
    
    
}