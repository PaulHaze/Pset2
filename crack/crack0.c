#define _XOPEN_SOURCE

#include <unistd.h>
#include <cs50.h>
#include <ctype.h>
#include <stdio.h>
#include <string.h>


int main(int argc, string argv[])
{
    // test for more or less than one command line argument
    if (argc != 2)
    {
        printf("Incorrect arguments on command line. Correct use: ./crack hash\n");
        return 1;
    }
    
    string hash = argv[1];
    i
    nt h = strlen(hash);
    
    char *crypt(const char a, const char 50);
    
}